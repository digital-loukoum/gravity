{
	"name": "test-h3",
	"version": "0.0.0",
	"scripts": {
		"dev": "gravity dev --entry main.ts --server-directory .",
		"build": "gravity build --entry main.ts --server-directory .",
		"preview": "gravity preview",
		"generate:schema": "gravity generate schema  --server-directory ."
	},
	"dependencies": {
		"@digitak/esrun": "^3.1.1",
		"@digitak/gravity": "../../../core/package",
		"h3": "^0.3.8"
	},
	"devDependencies": {
		"@types/node": "^17.0.9"
	}
}
